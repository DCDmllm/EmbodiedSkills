extends:
  - qwen3vl_pi05_online_seed_mix_grpo.yaml

name: qwen3vl_pi05_online_seed_mix_smoke

rollout:
  max_steps: 18
  episodes: 1
  group_size: 4
  artifact_prefix: clawvla_rl_online_seed_mix_smoke
  openpi_port_base: 9665

rollout_seed_mix:
  max_prompts: 1

openrlhf:
  total_epochs: 1
  total_training_steps: 1

checkpoint:
  save_freq: 1
  keep_last: 1

logging:
  wandb_mode: disabled

metadata:
  notes: One expert prompt, four real RoboTwin/OpenPI rollouts, and one full-parameter GRPO optimizer update.
