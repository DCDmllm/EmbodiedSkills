extends:
  - qwen3vl_pi05_libero_multitask_1update.yaml

name: qwen3vl_pi05_libero_multitask_1update_single_gpu

cluster:
  policy_gpus:
    - 2
  openpi_gpus: []
  robotwin_gpus:
    - 7
  ray_num_workers: 1

openrlhf:
  tensor_parallel_size: 1
  gpu_memory_utilization: 0.35
  max_num_seqs: 4

metadata:
  notes: Single-policy-GPU LIBERO multitask GRPO smoke used to validate the real rollout-to-update path without multi-rank NCCL collectives.
