# OpenPI/pi0.5 worker environment.
# Install this inside the existing openpi-torch-py312 environment.
-e .

numpy
pillow
pyyaml
torch
transformers==4.53.2
safetensors
sentencepiece

# The worker imports the training-aligned OpenPI source via OPENPI_PYTHONPATH.
# Keep JAX/Flax out of this torch worker unless a future OpenPI path explicitly
# needs them. The EmbodiedSkills runtime uses the torch OpenPI loader/hook.
