# OpenPI/pi0.5 worker environment.
# Install this inside the existing openpi-torch-py312 environment.
-e .

numpy
pillow
pyyaml
torch
transformers==4.53.2
safetensors
sentencepiece

# The current worker imports the training-aligned OpenPI source via PYTHONPATH:
# /mnt/linyutong/wangwai_mirror/pi0.5/src
#
# Keep JAX/Flax out of this torch worker unless a future OpenPI path explicitly
# needs them. Current ClawVLA pi0.5 path uses the torch OpenPI loader/hook.
